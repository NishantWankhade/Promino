<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/search_activity"
    tools:context=".searchActivityDealer">

    <SearchView
        android:id="@+id/searchViewDealer"
        android:layout_width="379dp"
        android:layout_height="74dp"
        android:background="@drawable/rounded_view"
        android:gravity="center"
        android:textAlignment="gravity"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.08" />

    <ScrollView
        android:id="@+id/scrollDealer"
        android:layout_width="402dp"
        android:layout_height="505dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/searchViewDealer"
        app:layout_constraintVertical_bias="0.831">

        <TextView
            android:id="@+id/dealerData1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="casual"
            android:textColor="#000000"
            android:textSize="20sp"
            android:textStyle="bold|italic" />


    </ScrollView>

    <Button
        android:id="@+id/search"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/rounded_view"
        android:fontFamily="casual"
        android:text="@string/search"
        android:textSize="20sp"
        android:textStyle="bold|italic"
        app:backgroundTint="#2196F3"
        app:layout_constraintBottom_toTopOf="@+id/scrollDealer"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.949"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/searchViewDealer" />

</androidx.constraintlayout.widget.ConstraintLayout>